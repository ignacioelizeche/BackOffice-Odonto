# ============ Database Configuration ============
DB_USER=postgres
DB_PASSWORD=postgres_dev_password
POSTGRES_DB=backoffice_odonto

# ============ Backend Configuration ============
ENVIRONMENT=development
SECRET_KEY=your-secret-key-change-this-in-production-should-be-at-least-32-chars
DATABASE_URL=postgresql://postgres:postgres_dev_password@db:5432/backoffice_odonto
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
ALLOWED_HOSTS=localhost,127.0.0.1,api.localhost

# ============ Frontend Configuration ============
NEXT_PUBLIC_API_URL=http://localhost:8000
NODE_ENV=development

# ============ Optional: Email Configuration (SMTP for Notifications) ============
 SMTP_HOST=smtp.gmail.com
 SMTP_PORT=587
 SMTP_USER=no-reply@elizeche.com
 SMTP_PASSWORD=zmgh qdqz caft bkfr
 SMTP_FROM=no-reply@elizeche.com

# ============ Optional: Application Settings ============
# DEBUG=false
# LOG_LEVEL=INFO
